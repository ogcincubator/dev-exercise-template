variables:
  DOCKER_DRIVER: overlay2

stages:
  - deploy

image:
  name: docker:23.0.5

services:
  - docker:23.0.5-dind
  
deploy:
  stage: deploy
  script:
    - docker compose -f docker-compose.yml up -d